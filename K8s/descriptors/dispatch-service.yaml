apiVersion: v1
kind: Service
metadata:
  name: dispatch
  namespace: robot-shop
spec:
  selector:
    app: dispatch
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP

